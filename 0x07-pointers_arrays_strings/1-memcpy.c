#include "main.h"

/**
 * _memcpy - library function it copeis memory area
 * @dest: memory area
 * @src: source
 * @n: length of src to be copied
 * Return: the pointer to dest.
*/

char *_memcpy(char *dest, char *src, unsigned int n);
{
	unsigned int arry;

	for (arry = 0; arry < n; arry++)

	{
		dest[arry] = scr[arry];
	}
	return (dest);
}

